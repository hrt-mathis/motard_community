<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        {% extends "app.scss" %}

        <title>Ajouter un utilisateur</title>
        
    </head>
    <body>
        {{ form_start(form) }}

        {{ form_row(form.userName) }}
        {{ form_row(form.userAge) }}
        {{ form_row(form.userDescription) }}
        
        <button type="submit">Enregistrer</button>
        
        {{ form_end(form) }}

        {{ form_errors(form) }}

        <a href="{{ path('user_index') }}">Retour à l'index</a>
        
    </body>
</html>


