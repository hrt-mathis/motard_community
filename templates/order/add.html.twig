<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Ajouter une commande</title>
        
    </head>
    <body>
        {{ form_start(form) }}

        {{ form_row(form.orderNumber) }}

        <button type="submit">Enregistrer</button>
    
        {{ form_end(form) }}

        {{ form_errors(form) }}

        <a href="{{ path('order_index') }}">Retour à l'index</a>
        
    </body>
</html>
